// En tu archivo personalizado de iconos (por ejemplo, customIcons.js)
import { icon } from "@fortawesome/fontawesome-svg-core"; // Asegúrate de importar la función "icon"

export const faGlobePointer = icon({
  prefix: "fas",
  iconName: "globe-pointer",
});
